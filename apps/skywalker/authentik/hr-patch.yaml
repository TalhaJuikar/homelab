apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: authentik
  namespace: authentik
spec:
  valuesFrom:
    - kind: ConfigMap
      name: authentik-values
      valuesKey: values.yaml