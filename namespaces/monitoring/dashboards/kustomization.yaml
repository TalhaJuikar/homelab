apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
# resources:
# - harbor.yaml
# - cloudnativepg.yaml
configMapGenerator:
  - name: infra-services-dashboard
    files:
      - traefik.json
      - harbor.json
      - cloudnative-pg.json
    options:
      labels:
        grafana_dashboard: "1"